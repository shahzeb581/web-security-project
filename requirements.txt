# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — GCUF Security Project | Shahzaib | Week 6
# OWASP A06 Fix: Vulnerable Components — all versions locked
# ─────────────────────────────────────────────────────────────────────────────

# Core Framework
Flask==3.1.0

# CSRF Protection (OWASP A04 Fix — Week 5)
Flask-WTF==1.2.2

# Rate Limiting — Brute Force Protection (OWASP A07 Fix — Week 4)
Flask-Limiter==3.9.0

# CORS Protection (Week 4)
Flask-Cors==5.0.0

# Security Headers — CSP, HSTS, X-Frame-Options (OWASP A05 Fix — Week 4)
flask-talisman==1.1.0

# Werkzeug (Flask dependency — pinned to avoid version disclosure)
Werkzeug==3.1.3

# Database
# SQLite is built into Python — no extra package needed

# Optional: For production bcrypt password hashing (upgrade from SHA-256)
# bcrypt==4.3.0
